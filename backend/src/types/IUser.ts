export interface UserInterface {
  name: string;
  email: string;
  //picture?: string; // URL da foto, se presente
  // iat?: number; // Issued at time
  // exp?: number; // Expiration time
  // aud?: string; // Audience
  // iss?: string; // Issuer
  // sub?: string; // Subject (geralmente igual ao uid)
  // phone_number?: string;
  // firebase?: {
  //   sign_in_provider?: string;
  //   identities?: {
  //     [key: string]: string[];
  //   };
  // };
}